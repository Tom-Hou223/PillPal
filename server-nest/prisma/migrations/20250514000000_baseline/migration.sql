-- Baseline migration: captures the existing database state as the Prisma starting point.
-- All tables were created before Prisma was introduced (brownfield project).
-- This migration is a no-op; it exists only for Prisma migration history tracking.
SELECT 'Baseline migration recorded' AS info;
