import { Text, Input, Textarea, Picker, Switch, Slider } from '@tarojs/components';

// Export common Taro components for easy import in pages
export { View, Text, Image, Button, Input, Textarea, ScrollView, Swiper, SwiperItem, Picker, Switch } from '@tarojs/components';
